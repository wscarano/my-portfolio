version: 0.2

phases:
  build:
    commands:
      - npm install
      - npm run webpack
artifacts:
  files:
    - index.html
    - favicon.ico
    - images/*
    - styles/main.css
    - dist/bundle.js
    